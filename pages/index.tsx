import type { NextPage } from 'next'
import Head from 'next/head'


const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>CSS Playground</title>
        <meta name="description" content="Playground to test animation in an isolated space" />
      </Head>
      <main>

      </main>
    </div>
  )
}

export default Home
